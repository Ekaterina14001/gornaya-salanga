# Serves pre-built Flutter web (run build-web.ps1 first).
FROM nginx:1.27-alpine

COPY build/web /usr/share/nginx/html
COPY web-nginx.conf /etc/nginx/conf.d/default.conf
RUN chmod -R a+rX /usr/share/nginx/html

EXPOSE 80
